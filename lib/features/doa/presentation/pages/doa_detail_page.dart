import 'package:flutter/material.dart';

class DoaDetailPage extends StatelessWidget {
  const DoaDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Doa Detail Page'),
        centerTitle: true,
      ),
    );
  }
}