import 'package:flutter/material.dart';

class DzikirDetailPage extends StatelessWidget {
  const DzikirDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dzikir Detail Page'),
        centerTitle: true,
      ),
    );
  }
}