import 'package:flutter/material.dart';

class SholatDetailPage extends StatelessWidget {
  const SholatDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sholat Detail Page'),
        centerTitle: true,
      ),
    );
  }
}